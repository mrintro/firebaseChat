import { initializeApp } from "firebase/app";

const validateEmail = (email) => {
    // validation logic.
    return true;
}

const validatePassword = (password) => {
    return true;
}

export {
    validateEmail,
    validatePassword
}